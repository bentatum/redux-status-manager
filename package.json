{
  "name": "redux-status-manager",
  "version": "1.0.2",
  "description": "success, pending and failure states for a given redux action type",
  "main": "dist/index.js",
  "repository": "https://github.com/bentatum/redux-status-manager",
  "author": "Ben Tatum <mail@benjtatum.com>",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist/",
    "build": "babel -d dist/ src/",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0-beta.40",
    "@babel/core": "^7.0.0-beta.40",
    "@babel/plugin-proposal-object-rest-spread": "^7.0.0-beta.40",
    "@babel/preset-env": "^7.0.0-beta.40"
  }
}
